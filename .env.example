# ====== Search Provider API Keys ======
# Get free keys:
#   Tavily: https://tavily.com (1000 req/month free)
#   Serper: https://serper.dev (2500 req free)
# Leave empty to use SearXNG only (fully free, no key needed)
TAVILY_API_KEY=
SERPER_API_KEY=

# ====== Internal Service Secrets ======
# Generate with: python3 -c "import secrets; print(secrets.token_hex(16))"
# Each value MUST be unique
SEARXNG_SECRET=
PROXY_API_KEY=
BROWSERLESS_TOKEN=
REDIS_PASSWORD=

# ====== HTTP Proxy (optional) ======
# For anti-bot fixed IP or bypassing geo-restrictions
# Supports: http://host:port, http://user:pass@host:port, socks5://host:port
PROXY_URL=
